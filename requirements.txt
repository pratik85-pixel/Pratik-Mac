# ZenFlow Verity — Python dependencies
# Install: pip install -r requirements.txt

# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
httpx==0.27.2               # async HTTP client + test client

# --- Data validation & config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Auth ---
PyJWT==2.9.0

# --- Database ---
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3
asyncpg==0.29.0             # async PostgreSQL driver
psycopg2-binary==2.9.9      # sync driver (Alembic migrations)

# --- Signal processing ---
numpy==2.1.2
scipy==1.14.1

# --- LLM / AI ---
openai==1.51.2
tiktoken==0.7.0             # token counting for context budget

# --- Scheduler ---
apscheduler==3.10.4             # nightly rebuild cron (02:00 UTC)

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# --- Dev utilities ---
rich==13.9.2                # pretty terminal output
