# Copy to `.env` in this directory (gitignored). Values are inlined at JS bundle time.
# Required for local Android Studio / Gradle release builds so the app hits the right API.
EXPO_PUBLIC_API_URL=https://api-production-8195d.up.railway.app
