diff --git a/node_modules/expo/android/build.gradle b/node_modules/expo/android/build.gradle
index 190ac19..39da830 100644
--- a/node_modules/expo/android/build.gradle
+++ b/node_modules/expo/android/build.gradle
@@ -1,5 +1,8 @@
 apply plugin: 'com.android.library'
 
+// Resolve Node before autolinking (subprojects can configure before root build.gradle finishes).
+apply from: new File(rootProject.projectDir, 'zenflow-node.gradle')
+
 // Import autolinking script
 apply from: "../scripts/autolinking.gradle"
 
@@ -14,9 +17,10 @@ static def versionToNumber(major, minor, patch) {
 }
 
 def getRNVersion() {
+  def nb = rootProject.ext.has('zenflowResolvedNode') ? rootProject.ext.zenflowResolvedNode : 'node'
   def nodeModulesVersion = providers.exec {
     workingDir(projectDir)
-    commandLine("node", "-e", "console.log(require('react-native/package.json').version);")
+    commandLine(nb, "-e", "console.log(require('react-native/package.json').version);")
   }.standardOutput.asText.get().trim()
 
   def version = safeExtGet("reactNativeVersion", nodeModulesVersion)
diff --git a/node_modules/expo/scripts/autolinking.gradle b/node_modules/expo/scripts/autolinking.gradle
index 929b7f0..cc90035 100644
--- a/node_modules/expo/scripts/autolinking.gradle
+++ b/node_modules/expo/scripts/autolinking.gradle
@@ -1,9 +1,12 @@
-// Resolve `expo` > `expo-modules-autolinking` dependency chain
-def autolinkingPath = ["node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })"]
+if (!rootProject.ext.has('zenflowResolvedNode')) {
+  apply from: new File(rootProject.projectDir, 'zenflow-node.gradle')
+}
+def nodeBin = rootProject.ext.zenflowResolvedNode
+def autolinkingPath = [nodeBin, "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })"]
 apply from: new File(
   providers.exec {
     workingDir(rootDir)
     commandLine(autolinkingPath)
   }.standardOutput.asText.get().trim(), 
   "../scripts/android/autolinking_implementation.gradle"
 )
